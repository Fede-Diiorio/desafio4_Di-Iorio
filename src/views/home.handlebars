<div>
    {{#each products}}
    <div>
        <h4>{{title}}</h4>
        <p>{{description}}</p>
        <p>Precio: {{price}}</p>
        <p>Stock: {{stock}}</p>
        <p>Código: {{code}}</p>
        <img src="{{thumbnail}}" alt="imagen de {{title}}">
    </div>
    {{/each}}
</div>